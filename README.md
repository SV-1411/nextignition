
  # NextIgnition (Copy) (Copy) (Copy)

  This is a code bundle for NextIgnition (Copy) (Copy) (Copy). The original project is available at https://www.figma.com/design/jLRRAugWKmIKjelHPjVOfc/NextIgnition--Copy---Copy---Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  